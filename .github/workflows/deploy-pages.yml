name: Deploy site to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow one deploy at a time; don't cancel a run that is mid-publish.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/configure-pages@v6

      # Static site, no build step — upload the site files as-is.
      # Only "BioViL - India" is published; anything else in the repo
      # (Brand - Germany, untracked working files) stays off the live site.
      - uses: actions/upload-pages-artifact@v5
        with:
          path: 'BioViL - India'

      - id: deployment
        uses: actions/deploy-pages@v5
